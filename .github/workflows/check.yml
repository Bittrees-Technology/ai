name: Checks
on: [push, pull_request]
permissions:
  contents: read
jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run check
      - run: node --check apps/remote-web/app.js
      - run: node --check apps/remote-web/controller.js
      - run: npm test
      - run: npm run contracts
      - run: git diff --exit-code -- contracts/
      - run: npm run build

  autonote-integration:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: integration
          POSTGRES_PASSWORD: ci-only
          POSTGRES_DB: autonote_test
        ports: [5432:5432]
        options: >-
          --health-cmd pg_isready --health-interval 5s --health-timeout 5s --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: Bittrees-Technology/autonote
          ref: 78023033431c1f45e6b8b203321e1b48215fd803
          path: .integration/autonote
          persist-credentials: false
      - uses: actions/checkout@v4
        with:
          repository: Bittrees-Technology/crm
          ref: daa4ef1ca690c67d0a837838a4a5575a0b3136eb
          path: .integration/crm
          persist-credentials: false
      - uses: actions/checkout@v4
        with:
          repository: Bittrees-Technology/news
          ref: 2d0059e75be2012bcdd19451b69c625ae695070d
          path: .integration/news
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm ci --prefix .integration/news
      - run: npm ci --prefix .integration/autonote
      - run: npm ci --prefix .integration/crm
      - name: Actual companion, AutoNote and CRM synthetic acceptance
        run: npx tsx scripts/autonote-integration-check.ts
        env:
          DATABASE_URL: postgresql://integration:ci-only@127.0.0.1:5432/autonote_test
          AUTONOTE_REPO: .integration/autonote
          CRM_REPO: .integration/crm
      - name: Actual News read, curation and publication journal acceptance
        run: npx tsx scripts/news-integration-check.ts
        env:
          NEWS_MCP_TEST_DATABASE_URL: postgresql://integration:ci-only@127.0.0.1:5432/autonote_test
          NEWS_REPO: .integration/news

  remote-status-integration:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: integration
          POSTGRES_PASSWORD: ci-only
          POSTGRES_DB: relay_test
        ports: [5432:5432]
        options: >-
          --health-cmd pg_isready --health-interval 5s --health-timeout 5s --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run build:remote
      - run: npx tsx scripts/remote-status-integration-check.ts
        env:
          REMOTE_TEST_DATABASE_URL: postgresql://integration:ci-only@127.0.0.1:5432/relay_test

  macos-package:
    runs-on: macos-14
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Native recovery lifecycle and shell typecheck
        run: bash scripts/check-macos-recovery.sh
      - name: Add-only synthetic Keychain installation
        run: bash scripts/check-macos-key-install.sh
      - name: Seed stale build outputs for rebuild regression
        run: |
          mkdir -p dist/apps 'dist/macos/Bittrees AI.app/Contents/Resources'
          touch dist/apps/stale-build-probe.js 'dist/macos/Bittrees AI.app/Contents/Resources/stale-bundle-probe'
      - run: bash scripts/package-macos.sh
      - name: Packaged recovery with disposable Keychain profile
        run: '"dist/macos/Bittrees AI.app/Contents/Resources/node" scripts/check-packaged-key-recovery.mjs "$PWD/dist/macos/Bittrees AI.app/Contents/Resources"'
      - name: Build untrusted native reader for endpoint-key denial check
        run: xcrun swiftc -parse-as-library tests/macos-private-key-denial.swift -o "$RUNNER_TEMP/private-key-denial" -framework Security
      - name: Packaged private endpoint keys with disposable Keychain slots
        timeout-minutes: 2
        run: '"dist/macos/Bittrees AI.app/Contents/Resources/node" scripts/check-packaged-private-keys.mjs "$PWD/dist/macos/Bittrees AI.app/Contents/Resources" "$RUNNER_TEMP/private-key-denial"'
      - name: Packaged private relay custody with disposable Keychain slots
        timeout-minutes: 2
        run: '"dist/macos/Bittrees AI.app/Contents/Resources/node" scripts/check-packaged-private-relay.mjs "$PWD/dist/macos/Bittrees AI.app/Contents/Resources" "$RUNNER_TEMP/private-key-denial"'
      - name: Deleted outputs must not survive packaging
        run: |
          test ! -e 'dist/macos/Bittrees AI.app/Contents/Resources/engine/dist/apps/stale-build-probe.js'
          test ! -e 'dist/macos/Bittrees AI.app/Contents/Resources/stale-bundle-probe'
      - name: Archive development bundle preserving executable permissions
        run: ditto -c -k --sequesterRsrc --keepParent 'dist/macos/Bittrees AI.app' dist/macos/Bittrees-AI-development.zip
      - uses: actions/upload-artifact@v4
        with:
          name: Bittrees-AI-macOS-development
          path: dist/macos/Bittrees-AI-development.zip
          retention-days: 7

  browser-private-protocol:
    runs-on: ubuntu-latest
    # The prior 933-case suite took 21 minutes; allow added full-page task flows
    # and previews while keeping per-test timeouts, retries and failure caps fixed.
    # The full browser suite plus retained previews can exceed 35 minutes.
    # Keep case timeouts/retries unchanged; allow artifact upload to finish.
    timeout-minutes: 45
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: integration
          POSTGRES_PASSWORD: ci-only
          POSTGRES_DB: browser_identity_test
        ports: [5432:5432]
        options: >-
          --health-cmd pg_isready --health-interval 5s --health-timeout 5s --health-retries 10
    env:
      BROWSER_TEST_DATABASE_URL: postgresql://integration:ci-only@127.0.0.1:5432/browser_identity_test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Prepare pinned browser key compatibility providers
        run: |
          git fetch --no-tags --depth=1 origin 84132a15d0ddf6027bc9bc8d8d9dbf4217fa69f0
          git fetch --no-tags --depth=1 origin 99a3c5e3a408305438f718e3326363b656234c9e
          git fetch --no-tags --depth=1 origin 3d692912ebecb14a04748b92226435ef1eb36e9f
          node scripts/prepare-legacy-browser-keys.mjs
          node scripts/prepare-legacy-browser-lifecycle.mjs
          node scripts/prepare-legacy-browser-private.mjs
          git fetch --no-tags --depth=1 origin b586ce3cd85f6320b51c78626498a3015a657e77
          node scripts/prepare-legacy-browser-consent.mjs
          git fetch --no-tags --depth=1 origin 9fbecfcc864843fc59fd43daf447f65178244be7
          node scripts/prepare-legacy-browser-composition.mjs
          git fetch --no-tags --depth=1 origin 515b1b369e0f0c4f62a7058484b059c81e3d59e7
          node scripts/prepare-legacy-browser-delivery.mjs
          git fetch --no-tags --depth=1 origin bb256d75932c214af57111536ab317231bcedd18
          node scripts/prepare-legacy-browser-conversation.mjs
      - name: Install disposable CI browser engines
        run: npx playwright install --with-deps chromium firefox webkit
      - run: npm run build:remote
      - name: Check independent retained browser conversation consent
        run: npm run test:browser-private -- tests/browser/browser-conversation-consent.spec.ts --max-failures=1
      - name: Check selected conversation permission reviews
        run: npm run test:browser-private -- tests/browser/conversation-permissions.spec.ts --max-failures=1
      - name: Check reviewed local task answers before full suite
        run: npm run test:browser-private -- tests/browser/task-answer.spec.ts --max-failures=1
      - name: Check task-linked Inbox privacy before full suite
        run: npm run test:browser-private -- tests/browser/inbox-task-review.spec.ts --max-failures=1
      - name: Check reviewed browser queue controls over real relay
        run: npm run test:browser-private -- tests/browser/browser-relay-queue-controls.spec.ts --max-failures=1
      - name: Check shipped command history controls before full suite
        run: npm run test:browser-private -- tests/browser/production-command-history.spec.ts --max-failures=1
      - name: Check durable command journal before full suite
        run: npm run test:browser-private -- tests/browser/browser-command-history.spec.ts --max-failures=1
      - name: Check narrow permission layout before full suite
        run: npm run test:browser-private -- tests/browser/production-browser-permissions.spec.ts --project=webkit --grep 'permission choices, review and history' --max-failures=1
      - name: Check reviewed task flow before full suite
        run: npm run test:browser-private -- tests/browser/production-browser-tasks.spec.ts --project=chromium --grep 'built task controls review exact input' --max-failures=1
      - name: Check connection fixture cleanup before full suite
        run: npm run test:browser-private -- tests/browser/news-publication.spec.ts --project=webkit --grep 'source-blocked feeds' --max-failures=1
      - name: Check relay permission uncertainty before full suite
        run: npm run test:browser-private -- tests/browser/production-relay-controls.spec.ts --max-failures=1
      - name: Check browser to native relay delivery before full suite
        run: npm run test:browser-private -- tests/browser/browser-relay-transfer.spec.ts --max-failures=1
      - name: Check reviewed private task delivery before full suite
        run: npm run test:browser-private -- tests/browser/production-relay-tasks.spec.ts --max-failures=1
      - name: Check future browser storage version fences before full suite
        run: npm run test:browser-private -- tests/browser/browser-endpoint-keys.spec.ts tests/browser/browser-peers.spec.ts tests/browser/private-protocol.spec.ts --grep 'database version' --max-failures=1
      - name: Check retained browser delivery history before full suite
        run: npm run test:browser-private -- tests/browser/browser-relay-delivery-history.spec.ts tests/browser/browser-delivery-upgrade.spec.ts --max-failures=1
      - name: Check Mac task delivery controls before full suite
        run: npm run test:browser-private -- tests/browser/mac-task-delivery.spec.ts --max-failures=1
      - name: Check Mac UI over authenticated local API and real relay
        run: npm run test:browser-private -- tests/browser/mac-task-delivery-native.spec.ts --max-failures=1
      - name: Check bounded relay queue recovery before full suite
        run: npm run test:browser-private -- tests/browser/relay-queue-inspection.spec.ts --max-failures=1
      - name: Check reviewed Mac queue controls over real local API and relay
        run: npm run test:browser-private -- tests/browser/mac-relay-queue-controls.spec.ts --max-failures=1
      - name: Check model question choices in local task forms
        run: npm run test:browser-private -- tests/browser/model-questions.spec.ts --max-failures=1
      - name: Verify browser and companion private-protocol interoperability
        run: npm run test:browser-private -- --max-failures=3
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: private-browser-results
          path: test-results/private-browser.json
          if-no-files-found: error
          retention-days: 7

      - name: Retain browser queue recovery previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: browser-queue-ui-preview
          path: test-results/browser-queue-*.png
          if-no-files-found: error
          retention-days: 7

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: private-results-ui-preview
          path: test-results/private-results-*.png
          if-no-files-found: error
          retention-days: 7

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: private-keys-ui-preview
          path: test-results/private-keys-*.png
          if-no-files-found: error
          retention-days: 7
      - name: Retain Mac peer review previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: private-peers-ui-preview
          path: test-results/private-peers-*.png
          if-no-files-found: error
          retention-days: 7
      - name: Retain Mac task permission previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: private-permissions-ui-preview
          path: test-results/private-permissions-*.png
          if-no-files-found: error
          retention-days: 7
      - name: Retain Mac device check previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: private-checks-ui-preview
          path: test-results/private-checks-*.png
          if-no-files-found: error
          retention-days: 7
      - name: Retain prerequisite failure previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dependency-failure-ui-preview
          path: test-results/dependency-failure-*.png
          if-no-files-found: error
          retention-days: 7

      - name: Retain complete local workspace previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: workspace-ui-preview
          path: test-results/workspace-*.png
          if-no-files-found: error
          retention-days: 7
      - name: Retain configurable model profile previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: model-profiles-ui-preview
          path: test-results/model-profiles-*.png
          if-no-files-found: error
          retention-days: 7
      - name: Retain import cleanup previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: import-cleanup-ui-preview
          path: test-results/import-cleanup-*.png
          if-no-files-found: error
          retention-days: 7
      - name: Retain task quality review previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: task-quality-ui-preview
          path: test-results/task-quality-*.png
          if-no-files-found: error
          retention-days: 7
      - name: Retain local execution limit previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: execution-limits-ui-preview
          path: test-results/execution-limits-*.png
          if-no-files-found: error
          retention-days: 7
      - name: Retain memory ranking explanation previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: memory-ranking-ui-preview
          path: test-results/memory-ranking-*.png
          if-no-files-found: error
          retention-days: 7
      - name: Retain local memory dependency previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: local-memory-dependencies-ui-preview
          path: test-results/local-memory-dependencies-*.png
          if-no-files-found: error
          retention-days: 7
      - name: Retain News connection review and read previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: news-connection-ui-preview
          path: test-results/news-connection-*.png
          if-no-files-found: error
          retention-days: 7
      - name: Retain reviewed News curation previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: news-curation-ui-preview
          path: test-results/news-curation-*.png
          if-no-files-found: error
          retention-days: 7

      - name: Retain reviewed News publication previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: news-publication-ui-preview
          path: test-results/news-publication-*.png
          if-no-files-found: error
          retention-days: 7
      - name: Retain reviewed Mail sending previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mail-send-ui-preview
          path: test-results/mail-send-*.png
          if-no-files-found: error
          retention-days: 7
      - name: Retain browser key setup and recovery previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: browser-key-controls-ui-preview
          path: test-results/browser-key-controls-*.png
          if-no-files-found: error
          retention-days: 7
      - name: Retain browser registration and key setup previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: browser-registration-ui-preview
          path: test-results/browser-registration-*.png
          if-no-files-found: error
          retention-days: 7

      - name: Retain production browser recovery previews
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: production-browser-recovery-preview
          path: test-results/production-recovery-*.png
          if-no-files-found: error
          retention-days: 7

      - name: Retain browser device identity controls previews
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: browser-peer-controls-ui-preview
          path: test-results/browser-peer-controls-*.png
          if-no-files-found: error
          retention-days: 7

      - name: Retain browser device check controls previews
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: browser-check-controls-ui-preview
          path: test-results/browser-check-controls-*.png
          if-no-files-found: error
          retention-days: 7

      - name: Retain browser task permission previews
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: browser-permission-controls-ui-preview
          path: test-results/browser-permission-controls-*.png
          if-no-files-found: error
          retention-days: 7

      - name: Retain reviewed browser task controls previews
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: browser-task-controls-ui-preview
          path: test-results/browser-task-controls-*.png
          if-no-files-found: error
          retention-days: 7

      - name: Retain Mac private relay connection previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mac-private-relay-ui-preview
          path: test-results/mac-relay-*.png
          if-no-files-found: error
          retention-days: 7
      - name: Retain command history controls previews
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: browser-command-history-ui-preview
          path: test-results/browser-command-history-*.png
          if-no-files-found: error
          retention-days: 7
      - name: Retain browser relay control previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: browser-relay-ui-preview
          path: test-results/browser-relay-*.png
          if-no-files-found: error
          retention-days: 7

      - name: Retain reviewed private task delivery previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: browser-relay-tasks-ui-preview
          path: test-results/browser-relay-tasks-*.png
          if-no-files-found: error
          retention-days: 7

      - name: Retain Mac task delivery previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mac-task-delivery-ui-preview
          path: test-results/mac-task-delivery-*.png
          if-no-files-found: error
          retention-days: 7

      - name: Retain Mac queue recovery previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mac-queue-ui-preview
          path: test-results/mac-queue-*.png
          if-no-files-found: error
          retention-days: 7

      - name: Retain task-linked Inbox previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: inbox-task-review-ui-preview
          path: test-results/inbox-task-review-*.png
          if-no-files-found: error
          retention-days: 7

      - name: Retain reviewed local task answer previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: task-answer-ui-preview
          path: test-results/task-answer-*.png
          if-no-files-found: error
          retention-days: 7

      - name: Upload model question form previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: model-question-ui-preview
          path: test-results/model-question-ui/*.png
          if-no-files-found: error
          retention-days: 7

      - name: Upload conversation permission previews
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: conversation-permission-ui-preview
          path: test-results/conversation-permission-ui/*.png
          if-no-files-found: error
          retention-days: 7
