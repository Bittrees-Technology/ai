# Reviewed browser task delivery

Continue the current Bittrees task workspace: pale blue #edf3f6 canvas, dark #183945 text, green #28685c section rule, blue #245e79 focus/action ink, white #ffffff review surface and #aabec5 separators. Avenir remains the body and heading family; existing 2.2rem/1.4rem/1.1rem heading hierarchy and 16px body avoid introducing another visual language.

Use the existing left-aligned history and review stage. Add private preparation next to manual preparation, sending on the exact saved task row, and one-message checking in the history actions. Reviews retain the full task text or exact saved task/Mac/deadline. Use one unchecked acknowledgement and a specific confirmation label, with separate result opening. No new cards, images, icons or motion.

Layout: task history and per-task actions | selected Mac, task text and review. On a narrow screen the existing stack presents history first, then full-width review; identifiers wrap and buttons remain keyboard reachable. Connection-only actions appear only when the host advertises the complete private policy. The same reset/blur/Escape/account/deadline fences cover new and existing actions.

Review against the brief: this is a continuation of the Mac companion's own task workflow. It exposes deliberate private delivery without inventing another dashboard. Server storage, authenticated Mac acceptance and a terminal result are distinct messages. Draft preparation does not send; checking a response does not reveal its text. Product copy names actions rather than internal transports.

Acceptance requires shipped-page browser tests for exact review, no automatic send, uncertain delivery, cancelled check, retained response and separate result opening; inspect actual desktop/narrow CI screenshots before acceptance. Acer runtime and news model stay unchanged.
